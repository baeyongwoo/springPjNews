<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.io.mapper.UserMapper">

	<insert id="join" parameterType="com.io.model.UserDTO">
        insert into userinfo (uemail, upwd, uname, did) 
        values (#{uemail, jdbcType=VARCHAR}, #{upwd, jdbcType=VARCHAR}, #{uname, jdbcType=VARCHAR}, #{did, jdbcType=VARCHAR})
    </insert>
    
    <delete id="deleteUser" parameterType="String">
    	delete from userinfo where uemail=#{uemail}
    </delete>
  
  
</mapper>
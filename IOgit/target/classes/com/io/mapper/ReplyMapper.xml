<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.io.mapper.ReplyMapper">
	<insert id="insert">
		insert into tbl_reply(rno,bno,reply)
		values(seq_reply.nextval, #{bno}, #{reply})
	</insert>
	
	<select id="getListWithPaging" resultType="com.io.model.ReplyVO">
	<![CDATA[
		select rno,bno,reply
		from(
			select /*+INDEX(tbl_reply idex_reply)*/
				rownum rn, rno, bno, reply
			from tbl_reply
			where bno=#{bno}
			and rno>0
			and rownum<=#{cri.pageNum}*#{cri.amount}			
		)
		where rn>(#{cri.pageNum}-1)*#{cri.amount}
	]]>
	</select>
	
	<select id="getCountByBno" resultType="int">
		select count(rno) from tbl_reply where bno=#{bno}
	</select>
	
	<select id="read" resultType="com.io.model.ReplyVO">
		select * from tbl_reply where rno=#{rno}
	</select>
	
	<delete id="delete">
		delete from tbl_reply where rno=#{rno}
	</delete>

</mapper>